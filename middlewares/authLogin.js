/* let authLogin = (req, res, next)=> {
    if(req.session.usuarioLogueado != undefined){
        next()
    } else{
        res.redirect("/login")
        next()
    }
} 

module.exports = authLogin */